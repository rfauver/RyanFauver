<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="text-center page-title">
      <%= link_to image_tag("title1.png"), '/' %>
    </header>

    <div id="nav-wrapper" data-spy="affix" data-offset-top="100">
      <nav class="navbar navbar-default" role="navigation">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
              <li <% if yield(:title) == "Portfolio" %>
                  class="active"
                <% end %>>
                <%= link_to "Portfolio", '/portfolio' %></li>
              <li <% if yield(:title) == "Blog" %>
                  class="active"
                <% end %>>
                <%= link_to "Blog",      '/blogs' %>
              </li>
              <li <% if yield(:title) == "Contact" %>
                  class="active"
                <% end %>>
                <%= link_to "Contact",   '/contact' %></li>
            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>
    </div>

    <div class="container">
      <div class="center hero-unit">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
